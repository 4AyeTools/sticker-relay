# 隐私说明

最后更新：2026-08-26

表情递是本地优先工具，不提供自建云端服务，不收集遥测、崩溃日志、通讯录、聊天
历史或表情内容。除微信和飞书功能本身所需的官方网络请求外，数据处理都在用户设备
上完成。

## 本地保存的数据

表情递会保存：

- 用户采集的表情图片；
- `manifest.json` 中的文件名、SHA-256、导入时间和飞书发送状态；
- 用户选择的表情库路径；
- 为恢复网页版微信文件传输助手会话所需的 Cookie、SyncKey 和会话字段；
- 按需下载的飞书官方 CLI 及其版本、SHA-256、来源信息。该组件使用独立的持久目录，
  Windows 默认位于 `%LOCALAPPDATA%\com.ayecode.wechatfeishustickers-components\lark-cli\`。

默认应用数据目录通常是：

- Windows：`%APPDATA%\com.ayecode.wechatfeishustickers\`
- macOS：`~/Library/Application Support/com.ayecode.wechatfeishustickers/`

用户可在界面中查看并更改表情库目录。飞书 CLI 的授权信息由官方 CLI 按其自身规则
保存；表情递不会读取或复制其中的访问令牌，只会启动官方 CLI 并读取命令结果。

## 登录状态保护

- Windows 使用当前 Windows 用户的 DPAPI 加密微信会话文件；其他系统账户无法直接
  解密。
- macOS 将微信会话保存在当前用户的 Keychain 中，磁盘只保留一个非敏感标记文件。
- 用户在关闭弹窗选择“退出微信并关闭”时，会请求微信退出并删除本地会话。
- 普通卸载只移除应用程序，保留设置、微信会话、飞书组件和表情库，方便重新安装后继续使用。
- Windows 卸载器勾选清理选项后，会删除设置、微信会话和位于默认目录的表情库；
  用户选择的自定义表情库不会被删除，但其目录设置会被清除。
- 已下载的飞书 CLI 可执行组件会保留在独立目录，重新安装后可直接复用；若用户希望
  彻底移除，可手动删除上述持久组件目录。
- 飞书官方 CLI 的授权与运行数据位于用户级 `.lark-cli` 目录，卸载器不会直接删除，
  避免影响其他使用同一官方 CLI 的开发工具。需要解除授权时应通过 CLI 退出登录。

## 网络访问

根据用户操作，应用会直接访问：

- 微信网页登录和文件传输助手：`login.wx2.qq.com`、`login.weixin.qq.com`、
  `szfilehelper.weixin.qq.com`；
- 飞书/Lark 官方授权与 Open API：由 `lark-cli` 发起；
- 飞书 CLI 版本和校验元数据：`registry.npmjs.org`；
- 飞书 CLI 二进制：`github.com`、GitHub 官方下载域名，失败时可切换到
  `registry.npmmirror.com`，且两种来源都必须通过同一份官方 SHA-256 校验。

应用不会把图片上传到项目作者的服务器。选择发送到飞书时，图片会通过飞书官方 CLI
上传到用户指定的自己、用户或群聊；这部分数据受飞书账号及组织策略约束。

## 删除和导出

用户可以在界面中删除单个、批量删除或清空本地表情；也可以导出 ZIP。删除本地文件
不会删除微信收藏，也不会撤回已发送的飞书消息。需要删除飞书中的数据时，应在飞书
客户端中操作。

## 微信非公开接口说明

微信采集功能基于网页版文件传输助手的非公开接口，不属于稳定、受支持的开放 API。
腾讯可能随时修改、限制或关闭相关能力。项目不会绕过验证码、风控、设备确认或账号
限制；用户应遵守微信服务条款，并仅迁移自己有权使用的内容。

## 联系与变更

隐私问题请通过 GitHub Issue 提出，但不要在公开 Issue 中粘贴 Cookie、Token、二维码、
日志原文或私人表情。安全漏洞请按 `SECURITY.md` 使用 GitHub Security Advisory 私下报告。
