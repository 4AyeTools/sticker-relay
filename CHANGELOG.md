# 更新日志

本项目采用语义化版本，日期使用东八区自然日。

## 0.4.1 - 2026-08-29

### 修复

- 适配 `lark-cli 1.0.92` 首次使用前必须执行应用配置的新前置条件；检测到
  `not_configured` 时，不再直接以授权失败结束；
- 首次连接飞书改为清晰的两阶段流程：先在浏览器中完成一次性应用配置，再自动打开
  飞书账号 OAuth 授权页；
- 应用配置进程改为后台运行并安全提取官方 HTTPS 地址，取消连接或关闭应用时会同步
  终止尚未完成的配置进程。

## 0.4.0 - 2026-08-28

### 新增

- 标题区增加应用版本和“检查更新”，启动后每 12 小时静默检查一次，也可随时手动检查；
- 发现新版本后在应用内展示版本号、发布日期和更新说明，并提供下载进度、失败提示及重试；
- 更新包下载完成后先安全保存微信会话，再安装并重启；本地表情库、飞书组件和迁移记录不受影响；
- 接入 Tauri 官方 Updater/Process 插件，Windows、macOS Intel 和 macOS Apple Silicon 均按当前架构选择更新包；
- Release 自动生成三平台 updater 产物、`.sig` 签名和 `latest.json`，并继续纳入 SHA-256 与 SBOM 发布流程。

### 修复

- 微信表情商店/专辑中仍以标准表情消息提供的内容，优先通过当前登录会话的消息图片接口下载；接口不可用时再回退到 CDN 地址。
- 消息图片接口改用微信文件传输助手当前官方参数格式：`MsgID`、`type=big` 与 `mmweb_appid=wx_webfilehelper`，并保留旧版参数作为兼容回退。
- 微信专辑/表情商店表情被网页版降级为无图片的占位消息时，不再重复请求空资源；改为说明网页版限制并提供手机端截图、保存后添加到飞书的降级指引。
- “最新动态”支持多行完整展示，不再截断较长的微信同步提示。

### 安全

- 应用内安装强制验证独立的 Tauri Updater 签名，私钥仅存本地离线备份和 GitHub Actions Secret，公钥编译进应用；
- Tauri 更新签名与可选的 Windows Authenticode、Apple Developer ID 签名相互独立。
- 微信消息图片接口仅在请求阶段携带会话参数，写入本地表情记录前会移除 `skey` 和消息 ID。

### 升级说明

- `0.3.1` 及更早版本尚未包含更新器，需要手动安装 `0.4.0` 一次；从 `0.4.0` 开始可使用应用内更新。

## 0.3.1 - 2026-08-27

### 修复

- 关闭应用时即时检查微信会话状态；未登录时显示普通关闭确认，不再出现“保留登录”或“退出微信”提示；
- 已登录或正在恢复会话时继续提供安全保留登录、退出微信并关闭两种选择；
- 标题栏关闭按钮与系统窗口关闭事件使用相同的状态判断逻辑。

## 0.3.0 - 2026-08-27

### 品牌

- 应用更名为“表情递 / StickerRelay”，并统一窗口、界面、安装包、便携版和发布产物命名；
- 保留原有应用标识和数据目录，升级后继续使用微信会话、飞书组件及表情库设置。

### 修复

- 修复安装或升级提示被通用布局规则覆盖后压住品牌区的问题；
- Windows 安装器可识别旧名称“咻咻搬”，沿用原安装目录并以更新模式移除旧程序；
- 飞书 CLI 改用独立的持久组件目录，旧版组件会自动迁移，清理应用数据后重新安装也无需再次下载；
- 卸载器明确说明清理范围：设置、微信登录和默认表情库会删除，飞书组件与自定义表情库保留；
- 飞书 CLI 授权目录 `.lark-cli` 不由卸载器直接删除，避免影响其他飞书开发工具。
- 修复 GitHub CI 和 macOS Release 缺少 `custom-protocol` 导致正式构建失败的问题；
- GitHub macOS 构建迁移到当前 Runner，并增加 RustSec 依赖漏洞审计。
- Windows 全新安装改用英文目录 `StickerRelay`，保留中文产品展示名；旧版升级继续沿用用户原安装目录。

## 0.2.5 - 2026-08-26

### 改进

- 首次安装或版本升级后的首次启动显示明确的完成提示；
- 提示会说明微信登录状态和本地表情已保留，并可由用户主动关闭。

## 0.2.4 - 2026-08-26

### 修复

- 允许国内加速线路从 `registry.npmmirror.com` 安全跳转至其文件 CDN；
- 为国内镜像入口、CDN 域名和伪造子域名增加白名单单元测试。

## 0.2.3 - 2026-08-26

### 改进

- 下载持续产生有效数据时不再受 180 秒总时长限制，避免慢速网络被误判为失败；
- 保留连续 30 秒无新数据的停滞检测，停滞后自动重试；
- 飞书官方组件下载调整为国内加速线路优先、GitHub Releases 备用。

## 0.2.2 - 2026-08-26

### 修复

- 流式下载连续 30 秒没有新数据时主动中断并进入下一次尝试；
- 单次下载总时长限制为 180 秒，避免连接存在但响应体永久挂起；
- 发布脚本严格选择当前版本的 NSIS 安装器，避免历史构建目录中的旧安装包被误复制。

## 0.2.1 - 2026-08-26

### 改进

- 飞书官方组件更新时在更新卡片内显示下载阶段、字节进度、下载源和尝试次数；
- 更新失败后保留错误信息，并提供明确的“重试更新”操作；
- GitHub Releases 与 npm 公共镜像各最多尝试两次，使用 1 秒、2 秒有界退避；
- SHA-256 校验失败会重新下载，安装采用临时文件与旧版备份，失败时不破坏现有组件；
- 修复 Windows 发布脚本漏启用 Tauri `custom-protocol` 导致安装版访问开发地址的问题。

## 0.2.0 - 2026-08-26

### 新增

- 飞书 CLI 可在图形界面中按需下载、检查更新和独立升级；
- 从飞书官方 npm 包读取版本及 `checksums.txt`，下载后二次验证 SHA-256；
- Windows x64/ARM64、macOS Intel/Apple Silicon 的组件资产选择；
- macOS Keychain 微信会话安全存储；
- Windows/macOS CI、Release、便携版、校验和和 SPDX SBOM 基础设施；
- Apache-2.0、隐私、安全、贡献、第三方许可证和发布文档。

### 安全

- 收紧 Tauri CSP 与 capability；
- 前端不再拥有任意打开 URL 的权限；
- 飞书授权地址限制为官方 HTTPS 域名；
- 默认安装包不再内置 43 MB 的未签名 CLI 二进制。

### 兼容

- 仍可发现旧安装包内置的 CLI，并允许一键迁移到独立托管组件；
- 修复 macOS 路径比较的大小写与分隔符逻辑。

## 0.1.7 - 2026-08-20

- 微信 Session/Cookie/SyncKey 持久化与 Windows DPAPI 加密；
- 启动自动恢复微信登录；
- 关闭确认、保留登录和服务端退出；
- Windows NSIS 安装包。
