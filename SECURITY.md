# 安全策略

## 支持范围

安全修复优先覆盖最新的稳定版本。早期测试版可能要求先升级后复现。

| 版本 | 安全更新 |
| --- | --- |
| 0.3.x | 支持 |
| 0.2.x | 仅评估严重问题 |
| 0.1.x 及更早 | 不再支持 |

## 私下报告漏洞

请优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability**。不要把
凭据泄露、任意代码执行、路径穿越或授权绕过等细节先发到公开 Issue。

报告建议包含：

- 受影响版本和操作系统；
- 可重复的最小步骤；
- 实际影响和攻击前提；
- 已去除 Cookie、Token、二维码及个人图片的日志；
- 如有，建议修复方向。

维护者确认并准备修复后，再协调公开时间。请勿测试不属于自己的微信、飞书账号或
组织数据。

## 当前安全边界

- React 页面不能直接访问任意文件或启动任意程序，只能调用显式注册的 Tauri Command。
- Tauri capability 使用最小权限；前端没有任意打开 URL 的 opener 权限。
- 飞书授权地址必须是 HTTPS 且属于 `*.feishu.cn` 或 `*.larksuite.com`。
- 飞书 CLI 只从固定官方域名下载，并按官方 npm 包内清单验证 SHA-256。
- 微信会话在 Windows 使用 DPAPI、在 macOS 使用 Keychain。
- CSP 禁止远程脚本、对象、表单和嵌入框架。

## 不属于漏洞的情形

- 微信非公开接口变更导致登录或同步失效；
- 微信或飞书自身的服务中断、账号风控或组织权限限制；
- 用户主动把表情发送给错误的飞书账号或群聊；
- 用户设备已被同账户高权限恶意软件完全控制后的本地数据访问。
